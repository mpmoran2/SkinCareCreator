<h2>Let's add a new step to your <i><b><%= @routine.name %></b></i></h2>

<%= render partial: '/errors', locals: { record: @step} %>

<%= render partial: 'form', locals: {step: @step, routine: @routine, url: routine_steps_path(@routine)}%>